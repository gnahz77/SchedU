# 课程导入导出功能说明

## 导入功能

### JSON格式1

支持同时导入课程表和时间表配置，格式如下：

```json
{
  "courses": [
    {
      "name": "高等数学",
      "position": "教学楼A101",
      "teacher": "张教授",
      "weeks": [1, 2, 3, 4, 5, 6, 7, 8],
      "day": 1,
      "sections": [1, 2]
    },
    {
      "name": "大学英语",
      "position": "教学楼B201",
      "teacher": "李老师",
      "weeks": [1, 2, 3, 4, 5, 6, 7, 8],
      "day": 2,
      "sections": [3, 4]
    }
  ],
  "timer": {
    "totalWeek": 20,
    "startSemester": "1694419200000",
    "startWithSunday": false,
    "showWeekend": false,
    "forenoon": 4,
    "afternoon": 4,
    "night": 4,
    "sections": [
      {
        "section": 1,
        "startTime": "08:00",
        "endTime": "08:50"
      },
      {
        "section": 2,
        "startTime": "09:00",
        "endTime": "09:50"
      },
      {
        "section": 3,
        "startTime": "10:00",
        "endTime": "10:50"
      },
      {
        "section": 4,
        "startTime": "11:00",
        "endTime": "11:50"
      }
    ]
  }
}
```

### 字段说明

#### courses（必填）
课程数组，包含所有课程信息。每个课程对象包含以下字段：

- `name`: 课程名称（字符串，必填）
- `position`: 上课地点（字符串，必填）
- `teacher`: 教师姓名（字符串，必填）
- `weeks`: 上课周数（整数数组，必填，如 [1,2,3,4]）
- `day`: 星期几（整数，必填，1-7表示周一到周日）
- `sections`: 节次（整数数组，必填，如 [1,2] 表示第1、2节课）

#### timer（可选）
时间表配置对象，如果不提供此字段，则只导入课程数据，保留现有时间表配置。

- `totalWeek`: 学期总周数（可选，整数，10-35之间）
- `startSemester`: 开学时间（可选，13位时间戳字符串）
- `startWithSunday`: 是否以周日为起始（可选，布尔值）
- `showWeekend`: 是否显示周末（可选，布尔值）
- `forenoon`: 上午课程节数（可选，整数，1-10之间）
- `afternoon`: 下午课程节数（可选，整数，0-10之间）
- `night`: 晚间课程节数（可选，整数，0-10之间）
- `sections`: 节次时间列表（可选，对象数组）
  - `section`: 节次（整数，1-30之间）
  - `startTime`: 开始时间（字符串，格式 "HH:MM"）
  - `endTime`: 结束时间（字符串，格式 "HH:MM"）

## JSON格式2

根上面的没有什么区别，只是不包含 timer 字段：

```json
[
  {
    "name": "高等数学",
    "position": "教学楼A101",
    "teacher": "张教授",
    "weeks": [1, 2, 3, 4, 5, 6, 7, 8],
    "day": 1,
    "sections": [1, 2]
  },
  {
    "name": "大学英语",
    "position": "教学楼B201",
    "teacher": "李老师",
    "weeks": [1, 2, 3, 4, 5, 6, 7, 8],
    "day": 2,
    "sections": [3, 4]
  }
]
```

## 使用场景

### 只导入课程
如果只想导入课程数据而不修改时间表配置，可以：
1. 使用格式2（纯课程数组）
2. 或使用格式1但不包含 `timer` 字段：

```json
{
  "courses": [
    // 课程数据...
  ]
}
```

## 注意事项

⚠️ **重要提示：**
- 导入课程数据会清空现有的所有课程！
- 导入时间表配置会覆盖相应的当前设置！
- 导入前请确保数据格式正确，避免导入失败

---

## 导出功能

### 导出内容

导出功能会将当前的所有数据保存为JSON文件（格式与导入相同），包括：

1. **课程数据**：所有已添加的课程信息
2. **时间表配置**：完整的时间表设置

### 使用方法

1. 打开应用，进入"我的"页面
2. 在"数据管理"分组中点击"导出课程"
3. 点击"导出"按钮
4. 选择保存位置和文件名（自动添加时间戳）
5. 导出完成

### 使用场景

- **数据备份**：导出课程数据保存为备份
- **设备迁移**：将数据迁移到新设备
- **数据共享**：与同学分享课程表设置

### 注意事项

- 如果没有课程数据，将无法执行导出
- 导出的文件可以直接用于导入功能
- 文件名会自动包含时间戳，避免覆盖

---

## 数据迁移流程

### 完整迁移步骤

1. **在旧设备上**：
   - 打开应用
   - 进入"我的" → "导出课程"
   - 保存JSON文件到可访问的位置

2. **传输文件**：
   - 通过云存储、邮件或其他方式传输JSON文件

3. **在新设备上**：
   - 安装应用
   - 进入"我的" → "导入课程"
   - 选择之前导出的JSON文件
   - 确认导入

4. **完成**：
   - 所有课程和设置已完整迁移
   - 可以立即使用

---

## 常见问题

### Q: 导出的文件在哪里？
A: 导出时可以自己选择保存位置，建议保存到易于访问的文件夹。

### Q: 可以只导出课程不导出时间表配置吗？
A: 目前导出功能会同时导出课程和时间表配置，确保数据完整性。如不需要时间表配置可以在导出后自行删除

### Q: 导入会覆盖现有数据吗？
A: 是的，导入会清空现有课程数据并应用新的时间表配置。建议导入前先导出备份。

### Q: 可以编辑导出的JSON文件吗？
A: 可以，导出的JSON文件是纯文本格式，可以用文本编辑器编辑，但需要保持正确的JSON格式。

---

## 技术说明

### 文件命名规则

导出的文件名格式：`schedu_export_[时间戳].json`

例如：`schedu_export_1700000000000.json`

### 兼容性

- 导出的文件可在任何安装了SchedU应用的设备上导入
- 支持跨平台迁移（Android、iOS、Web等）
- 向后兼容旧版本导入格式
